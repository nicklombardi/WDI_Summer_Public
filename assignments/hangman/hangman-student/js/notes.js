  checkLetters: function(guessedLetters){
    var correctLetters = [];
    var dashes = _.size(secretWord);
    var displayWord = [];
    _(dashes).times(function(i){ displayWord += "_"; });
    var incorrectLetters = [];
  }

    var word = "____";

    var dashes = _.size(this.secretWord);
    word = _(dashes).times(function( i += "_" );

  this.secretWord = wordList[Math.floor(Math.random() * this.wordList.length)];

this.secretWordUnderscore += (correctLetters)if

   var wrongLetters = _.reject(guessedLetters, this.secretWord);

       var wrongLetters = []
      for(var i = 0; i < guessedLetters.length; i++) {
        for(var n = 0; n < this.secretWord.length; n++) {
          if(guessedLetters[i] =!== this.secretWord[n]) [
            wrongLetterspush(guessedLetters[i]);
        }
        }
      }

    // loop through correct letters
    // substitute underscore at index where it matches
    for ( var i = 0; i < correctLetters.length; i++ ) {
      if () {
      }
    // return this.secretWordUnderscore;

      wordList: ['ruby', 'rails', 'javascript', 'array', 'hash', 'underscore', 'sinatra', 'model', 'controller', 'view', 'devise', 'authentication', 'capybara', 'jasmine', 'cache', 'sublime', 'terminal', 'system', 'twitter', 'facebook', 'function', 'google', 'amazon', 'development', 'data', 'design', 'inheritance', 'prototype', 'gist', 'github', 'agile', 'fizzbuzz', 'route', 'gem', 'deployment', 'database'],